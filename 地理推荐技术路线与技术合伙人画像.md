# 地理推荐技术路线与技术合伙人画像

## 一、当前基础已经具备什么

- 浏览器取得用户授权后，仅在内存中使用位置；中国境内从 WGS-84 转为高德使用的 GCJ-02。
- 高德周边搜索按“明确活动 + 当前需要”选择关键词，候选地点来自真实 POI，不让 LLM 编地点。
- 高德路线实测会校正距离、时间和交通方式；地图事实与人工核对数据有清楚来源标记。
- 高德逆地理编码已替代“四城矩形判断”，因此北京、上海、深圳、成都等中国大陆城市都能识别行政城市；四城矩形只在服务异常时保底。
- 到访反馈只有在围栏内停留得到验证后才可进入空间画像，且至少 5 次才参与排序。

高德官方逆地理编码接口：<https://lbs.amap.com/api/webservice/guide/api/georegeo>。

## 二、三阶段技术路线

### 现在到 1 万次有效到访：先把证据链做对

1. 每条推荐保存受限需求状态、候选来源、分数组成、实测路线和最终选择，不保存用户原话或轨迹。
2. 用接受率、实际出发率、到达率、停留后的变化分和“不合适原因”评估，不用点击率冒充有效推荐。
3. 分城市、时段、需求类型、明确活动和新/老地点看指标；任何总体提升都必须检查是否牺牲小城市或低样本地点。
4. 设置探索份额：主推荐保持稳健，备选中给低曝光但满足硬约束的地点有限探索机会。
5. 建立人工核对流程：地点身份、坐标、营业、可达性与属性估计分开标注来源和更新时间。

### 有稳定多城数据后：引入空间索引

1. 用 H3 将地点和粗粒度区域映射到分层六边形网格，做城市无关的邻域统计、供给密度、冷启动和匿名聚合。H3 使用 WGS84 并支持父子层级：<https://h3geo.org/docs/core-library/overview/>。
2. 在数据库启用 PostGIS，以 `ST_DWithin` 做半径候选、空间连接和索引过滤；不要用经纬度字符串或手写矩形承担生产查询。官方说明：<https://postgis.net/docs/ST_DWithin.html>。
3. 用户精确坐标仍不落库。分析只保存得到明确同意后的粗粒度网格，且设置最小群体门槛和保留期。
4. POI 属性、时间状态和到访证据分表，避免某次地图抓取覆盖人工或历史证据。

### 数据足够后：学习排序，但保留安全硬约束

1. 先用可解释的 pairwise/listwise learning-to-rank；特征只来自允许字段：需求匹配、路线、营业、空间画像、样本置信度、新鲜度与供给多样性。
2. 营业关闭、超出用户距离上限、明确不要的环境、地点身份不可信等继续做硬过滤，不交给模型“权衡”。
3. 用离线 NDCG@3/Recall@3 做初筛，线上以出发率、有效到访率、到访变化和后悔率为主；同时看 p95 延迟、无结果率、城市覆盖和类别集中度。
4. 新模型先影子运行，再小流量分层实验；不能只看均值，必须按城市和低样本群体检查。

## 三、现在不该做的事

- 不让 LLM 生成不存在的 POI、营业时间、距离或评分。
- 不收集连续轨迹来换“更智能”；一次性位置足够完成当前推荐。
- 不在样本不足时显示平均分或把编辑估计写成群体结论。
- 不急着上复杂向量数据库。当前瓶颈是到访证据量、地点核验和多城供给，不是向量检索规模。

## 四、需要加入的技术伙伴画像

### 优先背景

- 做过地图/LBS、POI 数据、空间索引、推荐系统或本地生活搜索中的至少一项。
- 能写生产后端和数据管道，不只是训练 notebook；熟悉 Python、PostgreSQL/PostGIS，懂 H3 或同类网格。
- 知道如何从隐式反馈中处理偏差、冷启动与探索/利用，不会把点击率直接当满意度。
- 对位置与情绪数据的隐私边界敏感，愿意让算法“不知道”一些不该知道的东西。
- 能与产品一起做小样本核验、线下访谈和数据标注，而不是只等待大数据。

### 30 天任务

1. 复核当前候选生成和排序日志，建立一套可重复的离线评估集。
2. 设计城市无关的 POI/空间画像数据模型与来源置信度。
3. 给出 H3/PostGIS 的最小迁移方案和性能基线，但不为技术而技术。
4. 与团队完成至少 20 条真实推荐的逐条错误分析，分类为理解、召回、事实、路线、排序和表达问题。

### 面试实操题

给候选人 30 条去标识的推荐记录和 15 个 POI，让其在 90 分钟内回答：

- 如何区分“没有召回正确地点”和“正确地点排序太低”？
- 样本量 2 的满分反馈与样本量 50 的中等反馈如何进入排序？
- 在不保存用户轨迹的前提下，如何衡量城市供给覆盖？
- 哪些约束必须永远是硬过滤？
- 如何设计一次不会被曝光位置偏差误导的小流量实验？

看重问题拆解、证据意识和边界感，不要求写大模型方案。

## 五、寻找渠道与文案

渠道优先级：团队可信熟人转介绍；高德/百度地图/本地生活/出行公司的技术社区；GeoHash/H3/PostGIS 开源贡献者；高校 GIS、城市计算、人机交互与推荐系统实验室；相关线下会议和 Meetup。

可直接发出的短文案：

> 我们在做“当下状态到真实空间”的推荐产品 Current：不诊断、不编地点，用地图事实、可解释排序和真实到访反馈，帮人找到此刻能接住自己的地方。现在已有可运行的多城附近搜索、路线和到访闭环，正在补空间数据与推荐评估。我们希望找到一位做过 GIS/LBS、POI 数据或推荐系统的技术伙伴，一起负责多城数据模型、空间索引和学习排序。比起“会调模型”，我们更看重生产工程、偏差意识、隐私边界和愿意逐条看真实失败案例。感兴趣可以先聊一次当前架构和 30 天最小任务。
