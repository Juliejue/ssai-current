# 外部服务开通清单

四个 Key，四件事只有你本人能做：注册、实名、绑卡、勾协议。
**拿到 Key 之后的所有配置和验证都可以交给 Claude Code。**

四个都不配，页面照样完整可走——只是语音是死的、距离是估算、数据重启就没。

**结论先说：内部测试、不商用的前提下，全部 0 元。**

之前标的唯一风险是「高德个人 Key 和 Vercel Hobby 都不允许商用」——
既然只做内部测试、不收费、不投广告，这条**不成立**，两个都可以直接用。
（等到真要收费或接商单那天再升级，见文末「什么时候必须花钱」。）

按「Demo 前必须有」排序：

| 顺序 | 服务 | 免费额度 | 我们大概会用掉 | 不配的后果 | 耗时 |
|---|---|---|---|---|---|
| 1 | 腾讯云实时 ASR | **5 小时/月**，每月 1 日重置 | ~2 小时/月 | **麦克风按下去报错**，语音优先是假的 | 实名 1–2 天 + 开通 20 分钟 |
| 2 | 智谱 GLM-4.7-Flash | **长期免费**（限并发不限 token） | 忽略不计 | 走本地规则，理解偏粗 | 10 分钟 |
| 3 | 高德 Web 服务 | 搜索 2,500/月 · 路径规划 15 万/月 | 搜索 ~30 次（一次性）· 路径 ~1,000/月 | 距离全是估算，导航和围栏核验都不出现 | 实名 1–2 天 + 逐点核对 2–3 小时 |
| 4 | Neon Postgres | 0.5 GB + 100 CU 小时/月，**不用绑卡** | 几 MB | 反馈重启即失，攒不出数据 | 15 分钟 |
| 5 | Vercel（部署） | Hobby 永久免费，**不用绑卡** | 远用不完 | 只能在自己电脑上看，手机打不开 | 10 分钟 |

**要实名的只有腾讯云、高德、智谱三家**（国内服务商都要，身份证 + 人脸，1–2 个工作日）。
Neon 和 Vercel 用 GitHub 账号直接登录，不实名也不绑卡。

用量估算按 100 人 × 3 次会话 = 300 次算。真超了也是几块钱的事——除了高德的商用限制。

> Key 拿到后**只贴给 Claude Code 或写进本地 `.env`**。
> `.env` 已在 `.gitignore` 里。**任何一个 Key 都不能进 HTML、前端 JS、GitHub、飞书文档。**
> 已经误提交过的 Key 一律当作泄露，去控制台重置，不要只删提交。

---

## 1. 腾讯云实时语音识别（最急）

**入口**：<https://console.cloud.tencent.com/asr>

**步骤**

1. 注册腾讯云账号 → **个人实名认证**（身份证 + 人脸，1–2 个工作日）。
2. 控制台搜「语音识别」→ 开通**实时语音识别**服务。
3. 左侧「应用管理」→ 记下 **AppID**。
4. 右上角头像 → 「访问管理」→ 「API 密钥管理」→ 新建密钥 → 记下 **SecretId** / **SecretKey**。
5. 「资源包管理」确认免费额度已发放。

**费用（2026-09 官方文档实测）**

- 实时语音识别 **免费 5 小时/月**，每月 1 日重置，**当月不用作废、不累积**。
- 超出后按 **3.2 元/小时**（0–299 小时/日 这一档）。
- 一句话识别另有 **5,000 次/月** 免费额度。

我们一次说话按 20 秒算，5 小时 ≈ **900 次**，Demo 加验证期完全够。

> **值得考虑的省钱方案**：如果 900 次/月不够，换成「一句话识别」有 5,000 次/月 免费，
> 按次数算比按时长划算得多。代价是失去实时回显（说话时那句「我在听」的逐字上屏）。
> 现在实现的是实时识别，要换我改一下就行。

**要给我的四个值**

```
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=
ASR_APP_ID=
ASR_ENGINE_MODEL=16k_zh        # 中文普通话，默认就是它
```

**坑**

- SecretKey 只在创建时显示一次，当场存好。
- **后付费默认是关闭的**，而且「预付费资源包耗尽将自动停服」。也就是说免费额度用完
  又没开后付费，语音会直接不能用。Demo 当天前记得确认额度还够，或者提前开后付费兜底。
- 音频是**浏览器直传腾讯**的，我们后端只签一个两分钟有效的临时 URL——SecretKey 不会进浏览器。这条别改，改了就等于把密钥发给所有访客。
- 麦克风需要 HTTPS 或 localhost。用 `python3 -m http.server` 开的 `127.0.0.1` 可以，局域网 IP 不行。
- 手机上测必须走 Vercel 的 HTTPS 域名。

**验证**（配好后我来跑）

```bash
curl -s http://127.0.0.1:8001/api/v1/asr/signature | head -c 200
```

返回带 `wss://` 的 URL 就通了；返回 503 就是环境变量没读到。

> ⚠️ **这一条只能在国内的真机上最终验证。**
> 实测把签名拿去连腾讯，返回 `code=6001「请检查是否开通跨境流量」`——
> 签名本身是被接受的（6001 不是鉴权错误），卡的是地理位置。
> 音频是浏览器直传腾讯的，所以只要**测试的人在中国大陆**，走的就是
> 控制台里那条「中国大陆实时流量：已开通 ✓」，不需要开通跨境流量（那要另外付费）。

---

## 2. 大模型 —— 通义 Qwen 国际站

**为什么不是智谱、也不是 DeepSeek**：两家都只有中国节点。我们的后端跑在 Vercel，
Vercel 没有中国大陆区域，所以调国内模型一定是跨境——这正是智谱那边持续 429 +
间歇性连接重置的原因。换 DeepSeek 是同一个坑。

通义 Qwen 有**新加坡节点**，OpenAI 兼容（代码一行不用改），中文母语级，
和我们的 Neon（新加坡）同区。

> 免费额度只在新加坡区。我们的函数在香港，香港到新加坡约 35ms——
> 之所以函数不一起放新加坡，是因为每个请求要调高德最多 3 次，
> 而高德在国内，香港离它更近。综合下来香港更划算。

**入口**：<https://modelstudio.console.alibabacloud.com>

**⚠️ 注册时最容易踩的坑：阿里云国际站和中国站是两套账号。**
必须注册 **alibabacloud.com**（国际站），不是 aliyun.com。用中国站账号登不进去。

1. 注册阿里云国际站账号。账号类型选 **个人账号**（「适用于个人学习和非商业用途」），
   和我们的高德个人 Key、Vercel Hobby 是同一套定位。用 Google / GitHub 注册最快。
2. 开通 Model Studio，**地域必须选 Singapore**。
   免费额度只在新加坡区发放，选香港或其他区就没有。90 天有效。
3. 建 API Key
4. 在控制台里**复制那一串 base URL 和模型 ID 原样给我**

第 4 步要注意：新加坡区的 base URL 现在是带 workspace 的格式
（形如 `https://{WorkspaceId}.ap-southeast-1.maas.aliyuncs.com`），
不是网上到处能搜到的那个旧地址。**以你控制台里显示的为准**，别照抄我写的。
模型 ID 同理（qwen-flash / qwen3.5-flash / qwen3.6-flash 都存在，
版本迭代很快）——我上次就是照着搜索结果写 `deepseek-chat`，结果那个已经下线了。

```
LLM_API_KEY=
LLM_BASE_URL=（控制台里那一串，末尾通常要加 /compatible-mode/v1）
LLM_MODEL=（控制台里列出的 flash 档模型 ID）
```

**关于绑卡**：Model Studio 的免费额度本身不需要绑卡。但阿里云国际站的账号注册
流程有时会要求填一张卡做身份验证——遇到了就填，免费额度内不会扣费。
（这一步我没亲自走过，以你实际看到的为准。）

**备选：Gemini 2.5 Flash-Lite** —— 想完全不花钱、不绑卡的话用它。
额度是明码硬配额（15 次/分、1000 次/天），比智谱那种「访问量过大」可预测得多，
而且原生支持 responseSchema。缺点是中文比 Qwen 略弱。

**坑**

- 模型只负责把自然语言转成**受约束的 JSON**，不负责选地点。别为了「更聪明」
  把推荐决策交给它——PRD §9.1 明确禁止。
- 输出契约已经做好：schema 校验失败会以 temperature=0 重试一次，限流和连接错误
  会退避重试，再失败自动退回本地规则，不会空屏。模型抽风不影响 Demo。

**验证**

```bash
curl -s -X POST http://127.0.0.1:8001/api/v1/interpret -H 'Content-Type: application/json' -d '{"text":"今天脑子很乱，不想见人，也不想花很多钱"}' | python3 -m json.tool
```

`"source": "model"` 就是模型在工作；`"source": "rules"` 说明没生效。

---

## 3. 高德地图 Web 服务 Key

**入口**：<https://console.amap.com/dev/key/app>

**步骤**

1. 注册高德开放平台 → **个人开发者认证**（身份证 + 人脸）。
2. 「应用管理」→ 创建新应用 → 添加 Key → **服务平台必须选「Web服务」**。
3. 记下 Key。

```
AMAP_WEB_SERVICE_KEY=
```

**免费额度**（2026-09 高德开放平台定价页实测）

| | 个人认证（非商业） | 企业认证（非商业，首年） | 超额 |
|---|---|---|---|
| 地点搜索 / POI | **2,500 次/月** | 5 万次/月 | 30 元/万次 |
| 路径规划 | **15 万次/月** | 300 万次/月 | 30 元/万次 |
| 地理/逆地理编码 | 15 万次/月 | 300 万次/月 | 30 元/万次 |

> 接手说明里写的是「地点搜索约 5,000 次/月」，**实测是 2,500**。以控制台为准。

我们的用量：搜索只在核对 26 个地点时用一次（~30 次），之后就不再调；路径规划每次推荐
最多 3 条，300 次会话 ≈ 900 次。**都是免费额度的零头。**

**坑**

- **「Web服务」和「Web端(JS API)」是两种 Key，选错了调不通**，报 `INVALID_USER_KEY`。
- **个人认证的 Key 不能用于商业用途。** 这是四个服务里唯一真正的风险——不是钱的问题，
  是资格问题。Demo、路演、验证期都算非商业，没问题；一旦开始收费或接商单，
  必须转企业认证 + 技术服务许可，否则可能被停 Key。这件事要提前问清楚商务。
- 这个 Key 是**服务端专用**，只能放 `.env` 和 Vercel 环境变量。放进前端就等于公开。
- 高德用 GCJ-02 坐标，浏览器定位用 WGS-84，中国境内差 **500 米以上**。围栏和导航的转换代码已经写好了，别绕过它。

### 拿到 Key 之后还有一步：逐点人工核对

**这一步我做不了，也不该由我做。** 代码里明确规定：不自动采用搜索结果第一名，只有人工确认过的地点才用于路线和导航。

我能做的是把候选拉出来给你看：

```bash
AMAP_WEB_SERVICE_KEY=你的Key .venv/bin/python -m scripts.resolve_amap_places
```

候选写进被 Git 忽略的 `backend_app/data/amap_candidates.json`。然后我们一起过 26 个地点，核对名称、地址、坐标对不对，确认一个我写一个进 `place_overrides.json`。

**核对完会同时解锁三件事**：真实步行距离和时间、高德导航跳转、**到访围栏核验**（评委第 3 条「你怎么证明他真去了」）。

一次核对 2–3 小时，可以分批做——核对几个就有几个生效，不用等全部。

**验证**

```bash
curl -s -X POST http://127.0.0.1:8001/api/v1/recommendations -H 'Content-Type: application/json' \
  -d '{"state":{"mood_id":"low"},"location":{"latitude":39.93,"longitude":116.40},"limit":3}' \
  | python3 -c "import json,sys; [print(r['place_name'], r['distance_source'], r['walking_minutes'], '围栏' if r['geofence'] else '') for r in json.load(sys.stdin)['recommendations']]"
```

`distance_source` 变成 `amap` 就说明真实路线通了。

---

## 4. Neon（PostgreSQL）

**入口**：<https://console.neon.tech>（或在 Vercel 项目里 Storage → Marketplace → Neon，会自动注入环境变量）

**步骤**

1. GitHub 账号直接登录，免费层不用绑卡。
2. 建 project，区域选 **Singapore / ap-southeast**，国内访问最快。
3. 项目建好后点 **Connect** 按钮（在 Project Dashboard 上），弹出「Connect to your database」。
   里面有个 **Connection pooling** 开关，**默认就是开的**，显示的那条就是 pooled 连接串。
   认准主机名里有 `-pooler`（形如 `ep-xxx-pooler.ap-southeast-1.aws.neon.tech`）。

   > 旧版控制台叫「Connection Details → Pooled connection」，现在没有这个叫法了，
   > 别在页面上找这几个字。

```
DATABASE_URL=postgres://...-pooler.../neondb?sslmode=require
```

**免费额度**（2026-09 Neon 官网实测）

- 存储 **0.5 GB / project**
- 计算 **100 CU 小时 / 月 / project**
- 项目数上限 100，每个项目 10 个分支
- 出站流量 5 GB
- **永久免费，不是试用，不用绑卡**

我们存的是结构化小记录，300 次会话大概几 MB，计算时间也远用不完 100 CU 小时。
第一个付费档 Launch 是按量计费无月费：$0.106/CU 小时 + $0.35/GB 月。

**坑**

- **一定要用 pooled 那条。** Serverless 每次冷启动都新建连接，直连会很快把连接数打满。
- 免费层会在闲置几分钟后休眠，第一次请求慢 1–2 秒。Demo 前先点一下预热。
- 三套环境（Development / Preview / Production）分开配，别共用一个库。

**填 Key 的省事办法**

仓库根目录有 `填Key.sh`：

```bash
bash 填Key.sh
```

输入时不显示、不进终端历史、不进 Git，六个 Key 一次填完，不想填的直接回车跳过。
这样 Key 不用经过聊天窗口。

**配好后我来跑**

```bash
.venv/bin/python scripts/migrate.py
.venv/bin/python -m scripts.seed_places
```

**存什么 / 不存什么**

| 会存 | 不会存 |
|---|---|
| 结构化需求（`NeedState`） | 倾诉原文 |
| 推荐排名、得分、各维度分解 | 原始语音 |
| 接受／拒绝／导航／到访状态 | 用户经纬度 |
| 变化分数、影响因素 | 私密反馈正文 |
| 哪一环对/错（`mismatch_stage`） | 姓名等可识别信息 |
| 在场证明等级与停留时长 | |

---

## 全部配好之后

1. 我把四组值写进本地 `.env`（不提交）。
2. 你在 Vercel → Settings → Environment Variables 里配同样的值，**Preview 和 Production 分开**。
3. `ALLOWED_ORIGINS` 要加上 Vercel 给的预览域名，否则浏览器会被 CORS 挡掉。
4. 我跑一遍迁移 + 全量测试 + 浏览器端到端。
5. 你在**手机上**走一遍：语音说一句 → 看推荐 → 点带我去 → 真的走过去 → 回来给反馈。

第 5 步没做完，就还不算做完。

---

## 5. Vercel（部署，让手机能打开）

**入口**：<https://vercel.com>

1. 用 GitHub 账号登录（不用绑卡、不用实名）。
2. Import Git Repository → 选 `Juliejue/ssai-current`。
3. Settings → Environment Variables 填上面那几个 Key，**Preview 和 Production 分开填**。
4. 每次 push 会自动生成一个 Preview 链接，手机上直接打开。

**Hobby 免费额度**（2026-09 官方文档实测，我们的用量连零头都算不上）

- 函数调用 100 万次/月 · Edge 请求 100 万次 · 数据传输 100 GB
- 每天 100 次部署 · 200 个项目
- Web Analytics 5 万事件/月

**关于「不允许商业用途」**

Vercel 把商业用途定义为「任何以**获取经济利益**为目的的部署」，列举的例子是：
向访问者收款、售卖商品或服务的广告、收钱替人做站或托管、以联盟链接为主要目的、投放广告。

我们内部测试：不收费、不卖东西、不投广告、不放联盟链接——**一条都不沾**。可以用 Hobby。

两点要知道：

- 条款里有一句「包括拿钱写这段代码的雇员或外包」。这条写得很宽，
  但整条规则的落点是「这个部署本身是不是在为谁赚钱」。做不收费的内部验证不属于。
  真不放心可以直接问 Vercel 支持，他们文档里就是这么建议的。
- **Hobby 是个人账号，没有团队协作功能。** 只有你一个人部署没问题；
  要让组员也能进 Vercel 后台管环境变量，得升 Pro（$20/人/月）。
  内部测试阶段更省事的办法：你一个人管 Key，别人看 Preview 链接就行。

---

## 6. 26 个地点核对（不用注册，但只能人来做）

这一步没有网站要注册，它是**拿到高德 Key 之后的一次性人工活**。

代码里有条硬规矩：不自动采用搜索结果第一名，只有人工确认过的地点才用于路线、
导航和到访围栏。我可以把候选拉出来，但「这个搜索结果是不是就是那家店」只能你来判断——
判断错了，用户会被导航到错的地方。

流程：我跑 `scripts/resolve_amap_places`，把 26 个地点的候选（名称、地址、坐标）
列给你；你逐个看对不对；确认一个我写一个进 `place_overrides.json`。

- 耗时：一次 2–3 小时，**可以分批**，核对几个就生效几个。
- 花费：0（用掉大约 30 次搜索额度，免费额度是 2500 次/月）。
- 核对完同时解锁三件事：真实步行距离与时间、地图导航跳转、到访围栏核验。

---

## 7. 手机端到端（不用注册）

也不用注册任何东西。Vercel 部署好之后，用手机浏览器打开 Preview 链接，
完整走一遍：说一句话 → 看推荐 → 点带我去 → 真的走过去 → 回来给反馈。

必须在手机上、必须走 HTTPS（Vercel 的链接就是），因为：

- 麦克风权限只在 HTTPS 或 localhost 下给，局域网 IP 不行
- 地图跳转要在真机上才能唤起高德 App
- 到访围栏要真的移动才测得出来

**这一步没做完就不算做完。** 前面所有东西我都只在电脑浏览器里验证过。

---

## ⚠️ 部署前先测一件事：国内能不能打开 vercel.app

`*.vercel.app` 在中国大陆的可达性一直不稳定。**这件事会卡住整个手机端到端验证**，
而且发现得越晚越糟——别等到 Demo 当天。

部署好之后第一件事：**用手机 4G（不是 WiFi、不开代理）打开那个 Preview 链接。**

- 打得开 → 继续
- 打不开 → 把 heyjue.com 或它的子域名绑到这个 Vercel 项目上，用自有域名访问

不管走哪条，`ALLOWED_ORIGINS` 都要把最终那个域名加进去，否则浏览器会被 CORS 挡掉。

---

## ⚠️ 一件影响架构的事：Vercel 不在中国大陆

这次配置过程中发现，开发机的出口 IP 在境外。由此暴露的现象，同样会出现在
**部署到 Vercel 之后**——Vercel 没有中国大陆区域，函数一定在境外跑。

| 谁调谁 | 在哪跑 | 影响 |
|---|---|---|
| 后端 → 高德 Web 服务 | Vercel（香港） | 跨境，但香港已是能选的最近的一个 |
| 后端 → 通义 Qwen | Vercel（香港）→ 新加坡/香港 | **同区**，这也是不选 DeepSeek 的原因 |
| 后端 → Neon | Vercel（香港）→ 新加坡 | 约 30ms |
| **浏览器 → 腾讯 ASR** | **用户手机（国内）** | **不跨境**，走「中国大陆实时流量」。后端只做签名，不碰音频——这个设计正好躲过了跨境问题 |

两个结论：

1. **ASR 的浏览器直传设计是对的**，不只是为了不泄漏 SecretKey，也顺带避开了跨境流量。
1b. **函数区域已经钉在香港**（`vercel.json` 里 `"regions": ["hkg1"]`）。
   Hobby 只能选一个区，默认是华盛顿——不改的话每个请求都要横跨太平洋两次。
   香港是四个依赖（用户、高德、Qwen、Neon）综合起来最短的那个点。

2. **高德和 LLM 的跨境延迟要在真机上量**。PRD §8 要求「推荐返回 < 3s」，
   本地实测都是几百毫秒，但那是不带高德实测路线的。等地点核对完、真正开始调
   高德路线规划之后，要重新量一次。真超了，方案是给路线结果做缓存
   （同一个地点的步行时间不会一分钟变一次）。

---

## 什么时候必须开始花钱

只有下面任何一条成立时：

| 触发条件 | 要升级什么 | 大概多少 |
|---|---|---|
| 开始向用户收费 / 投广告 / 卖东西 | Vercel Pro + 高德企业认证 | Vercel $20/人/月起；高德另议 |
| 组员也要进 Vercel 后台 | Vercel Pro | $20/人/月 |
| 语音超过 5 小时/月 | 腾讯云后付费 | 3.2 元/小时 |
| 单个地点搜索超过 2500 次/月 | 高德超额 | 30 元/万次 |
| 数据超过 0.5 GB | Neon Launch | 按量，$0.35/GB·月 |

内部测试阶段，这五条一条都不会碰到。

---

## 一句话总结

**内部测试、不商用，全部 0 元，一张卡都不用绑。**

你本人要做的只有两件事：**三家国内服务商的实名认证**（腾讯云、高德、智谱），
以及**26 个地点的人工核对**——后者不是我不愿意做，是代码里明确规定不能由程序认领。

其余每一步——配置、迁移、跑候选、验证、部署——都可以直接扔给我。

---

*费用数据核对于 2026-09-09，来源为各家官方文档与定价页。价格会变，以控制台为准。*
