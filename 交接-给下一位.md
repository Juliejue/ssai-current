# Current / 此在 · 交接说明（2026-09-10）

上一份交接是 `交接-背景说明.md`（8 月写的，代码部分基本全过时了）。**这一份覆盖它。**

产品要求以 PRD 为准，本文只讲代码状态和踩过的坑。
**PRD 不在仓库里**，在 `/Users/juer/Desktop/此在-Current-PRD-V1.5.md`（项目目录的上一层）。
动手前先完整读一遍——本文提到的 FR 编号、US 编号、守则编号全部出自它。

---

## 0'. 2026-09-12 这一夜改了什么（本节最新，与下文冲突以本节为准）

### 候选地点不再是写死的 26 个

`backend_app/discovery.py`：按用户此刻的位置做高德周边搜索，把 POI 变成和人工地点
同一个形状，交给同一套打分逻辑。**北京以外也能用**（上海、深圳实测过）。

**不要改回分类编码。** 试过了，走不通：高德的类目是给「找一家店」用的，
要 `110204 纪念馆` 会给你陈独秀旧居和劳动人民文化宫——类目对了，气质全错。
我们的人是 18–38 岁、在意心理空间的女性。
关键词才是能控制气质的地方，而且**关键词跟着 NeedState 变**（`keywords_for()`）：
想动手的搜「陶艺/黑胶/中古店」，想发呆的搜「书店/剧场/茶室」。

词表对着 Threads 上的真实求助帖定的。有个旁证值得记下来：
那条「北京有什么散步之地」206 赞帖子底下被点名的是 postpost、杨梅竹斜街、
soloist、什刹海、做書、亮马河、郎园——**几乎全在原有的 26 个里**。选品是对的。

连锁店在 `CHAIN_BRANDS` 里硬拉黑。星巴克瑞幸出现在这个产品里就是走错了方向。

### 两类地点故意不平权

| | 人工核对过的 26 个 | 现场搜出来的 |
|---|---|---|
| 身份 | 人一个个核对过 | 高德自己的 POI 记录 |
| 导航 | 给 | 给（坐标就是这个 POI 自己的） |
| 围栏在场证明 | 给 | **不给** |
| 「小在眼中的这里」 | 人写的 | 没有，不编 |
| 匿名汇总 | 攒够 5 条 | 永远没有 |

界面必须看得出区别。**把搜索结果说成是我们核对过的，是这里最不能犯的错。**

### 模型现在多了一份工作：写文案

`backend_app/narrate.py`：按「你此刻的状态 + 这个地方的名字」写 action 和 why_now。
硬规则——**只许用地名和类别里已有的信息**，不许编这个地方的事实（它最容易在这里
顺口编出「有一面很好的墙」）。写不出来就退回类别模板，绝不编。

**排序仍然不交给模型（守则 7 没变）。** 它只负责把已经排好的地点说成人话。

### 实景图取代了符号图

原来 HTML 注释里的理由是「6+ 个地点无法拿到稳定合规的匹配图源」。那在当时成立，
但前提变了：这些地点都有人工核对过的 POI ID，按 ID 取的照片就是这一家。
`scripts/fetch_place_photos.py`（幂等）已经给 20/21 个补上了。
拿不到照片仍然退回符号——宁可抽象，也不放别处的图冒充这里。

推荐卡上还加了一张地图。**必须走 `/api/v1/map/static` 这个服务端代理**：
高德静态地图接口把 key 放在 URL 里，直接让浏览器请求等于把 key 贴在页面上。

### 英文版

分两层，因为用户读到的内容大半不是界面文字：
- 后端按 `lang` 出英文（`backend_app/i18n.py`）：状态标签、理由链、代价、
  澄清问题、纠错选项，以及 narrate 的独立英文 prompt（**字数上限单独设**——
  英文一个词占好几个字符，按汉字的尺子量会把好句子全砍掉）。
- 26 个地点整体译成 `data/places.en.json`（`scripts/translate_places.py`，可重跑）。

前端**不改渲染函数**，在 `render()` 之后走一遍 DOM 换文案（`applyLang`）。
1000+ 条中文散在模板字符串里，逐个包 `T()` 要动几乎每一行渲染代码。
这一层是外挂的：翻译出问题，关掉开关就回到完好的中文版。
两三个字的碎片只在「这个节点已经有英文」时才替换——
「刚从 X 回来吗？」被拼成过「刚从postpComing back?」，就是碎片乱替换的结果。

现场搜出来的地点**保留中文名**：人要照着招牌找过去，翻成英文反而找不到。

### 还没解决的

- **延迟**：本机首次推荐 16–17 秒（跨境搜六组关键词 + 生成文案）。
  已做 `/interpret` 期间预热 + 按关键词缓存，第二次快很多。香港区要重新量。
- 记录页还有几条演示种子数据是中文。
- 测试现在是 **101 条 Python + 13 条 Node**（下文写的 93 是旧数）。

---

## 0. 一分钟定位

```
目录    /Users/juer/Desktop/ssai 情绪空间项目
仓库    https://github.com/Juliejue/ssai-current
分支    codex/current-backend-foundation   （未合 main，未部署）
最新    998eb3c
PR      https://github.com/Juliejue/ssai-current/pull/3   （open）
```

```bash
cd "/Users/juer/Desktop/ssai 情绪空间项目"
set -a && . ./.env && set +a          # ← 必须。不加载 .env 什么都跑不起来
.venv/bin/uvicorn backend_app.main:app --port 8001 &
python3 -m http.server 8000 &
open "http://127.0.0.1:8000/此在-current-原型.html"
```

检查：

```bash
.venv/bin/pytest -q                      # 93 passed
node --test tests/reach-policy.test.js   # 13 passed
node --check current-client.js && node --check reach-policy.js
python3 -m compileall -q backend_app scripts
node scripts/extract_place_data.mjs      # 幂等，从 HTML 重新生成 places.json
```

**不要用 `file://` 双击 HTML。** 后端、地图、数据库全部失效。

---

## 1. 凭据：已经全部配好，都在 `.env` 里

`.env` 在 `.gitignore` 里，**不在仓库**。Julie 本机已有全套五项：

| 变量 | 服务 | 状态 |
|---|---|---|
| `DATABASE_URL` | Neon Postgres（新加坡） | ✅ 迁移 001–004 已跑，26 地点已 seed |
| `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` | 通义 Qwen 国际站（新加坡） | ✅ `qwen3.8-flash` |
| `AMAP_WEB_SERVICE_KEY` | 高德 Web 服务 | ✅ 搜索 + 路线都通 |
| `TENCENT_SECRET_ID/KEY` + `ASR_APP_ID` | 腾讯实时 ASR | ⚠️ 签名被接受，但**只能在国内真机上最终验证**（见 §3） |
| `ALLOWED_ORIGINS` | CORS | 部署后必须把最终域名加进去 |

补 Key 用 `bash 填Key.sh`（不回显、不进终端历史、不进 Git）。

> **安全提醒**：Julie 在聊天里贴过腾讯云的**登录密码**，已提示她修改。
> 如果还没改，再提醒一次。API 密钥和登录密码是两回事。

---

## 2. 现在真的能跑的东西

主旅程（`此在-current-原型.html` + `current-client.js`）：

```
#/talk    语音优先第一屏（麦克风按钮 / 打字折叠）
#/echo    小在复述「我猜你更接近…」+ 模型给的证据 + 至多一次追问
#/fix     四环纠错：状态 / 需求 / 条件 / 地点
#/offer   1 主推 + 2 备选：动作标题 · 理由链 · 到达 · 营业 · 代价
行程条    带我去 → 在路上 → 我到了 → 我离开了（存 localStorage）
#/visit   30 秒反馈 + 哪一环对/错
#/records 记录 +「下次推荐我会怎么改」
#/settings 分层授权 / 打扰设置 / 清空数据
#/reach   主动触达（演示入口在设置里）
#/recheck FR-31 延迟复看
#/safe    强痛苦转介
```

**21 个地点已完成人工核对**，因此这些是真的：高德实测步行距离、地图三选跳转
（高德 GCJ-02 / Apple·Google WGS-84，已转换）、到访围栏核验、高德提供的营业时间。

数据库四张表都在写：`product_events`、`visit_outcomes`、`recommendation_decisions`、`places`。

---

## 3. 六个踩过的坑，**别再踩一遍**

### ① 开发机和 Vercel 都在境外

出口 IP 在境外（实测 Datacamp 机房）。Vercel 也没有中国大陆区域。所以：

- 后端调高德、调 LLM **都是跨境**。`map_provider.py` 和 `interpretation.py` 里的
  传输层重试**不是防御性冗余，是生产必需**，别删。
- 腾讯 ASR 从境外测会返回 `code=6001 跨境流量未开通`——**那不是配置错误，是地理位置**。
  音频是浏览器直传腾讯的，用户在国内就走「中国大陆实时流量」。
  **只能在国内真机上验证。**
- `vercel.json` 里 `"regions": ["hkg1"]` 是刻意的（Hobby 只能选一个区，默认华盛顿）。

### ② Qwen3 必须发 `enable_thinking: false`

不发的话 18 秒，发了 5 秒。直接决定能不能满足 PRD §8 的「推荐返回 < 3s」。
这是 Qwen 专有参数，`_call_model` 里按模型名条件下发，别无脑发给别的厂商。

### ③ CORS：`localhost` 和 `127.0.0.1` 是两个源

`ALLOWED_ORIGINS` 少写一个就是浏览器端 `Failed to fetch`，而 **curl 测不出来**
（curl 不管 CORS）。部署后把预览域名/自有域名也加进去。

### ④ 高德的营业时间会返回 `24:00`，还会多时段

`time(24, 0)` 在 Python 里越界，真实数据一写进去 20 个测试全红。
多时段（`00:00-02:00 08:00-23:00`）不能压成一个区间，否则会把凌晨 2–8 点说成开着——
「说开着其实关着」是两种错里更糟的那一种。两处都在 `opening_hours.py` 修好了。

### ⑤ 模型会把 prompt 里的占位符和字段名吐给用户

第一版让模型给证据，它原样抄了我的格式串「所以我怎么理解：」，还把 `mood_id`
说给用户听。`_quotes_the_user()` 是代码侧护栏：「」里的片段必须真的出现在
用户原话里，且不含字段名——对不上整条丢掉。**不要为了「证据更丰富」放宽它。**

### ⑥ 别只用 curl 验收

这一轮好几个问题（CORS、界面重复、样式撞车）curl 全是绿的，只有在浏览器里
真走一遍才看得见。PRD 交接要求写得很清楚：**以用户实际可体验为完成标准。**

---

## 4. 不可以退让的边界（改动前先读）

这些不是风格偏好，是 PRD §5.2 / §9 的硬约束，而且都有测试守着
（`tests/test_redline.py`，93 条里占 30 条）：

1. **不诊断** —— 不用临床或人格词。测试会扫描整个原型文案。
2. **不评判** —— 情绪变差叫「这次没接住」，色阶负端**不能用红**（按 RGB 断言）。
3. **不展示单条反馈** —— US-04。只给聚合 + 样本量。曾经有个匿名到访流，已拆掉。
4. **地图身份必须人工核对** —— 不自动采用搜索第一名。搜「水果店 fRUITYSHOP」
   前五名全是真卖水果的店，自动认第一名会把用户导到国贸的水果摊。
5. **在场证明只降不升** —— 浏览器报的等级不可信，`presence.py` 只负责降级。
   没有人工核对坐标的地点没有围栏。
6. **不存原话、不存坐标** —— 围栏判定在浏览器里完成，只上传结论。
   日志和事件属性走白名单。留痕只留输入摘要（sha256 前 16 位）。
7. **不把推荐决策交给 LLM** —— 模型只做「自然语言 → 受约束 JSON」。
   排序逻辑是我们自己的（`recommender.py`）。

---

## 5. 立刻要做的三件事

### A. 5 个地点还没核对（只有 Julie 能答）

`backend_app/data/place_overrides.json` 里缺这五个：

| placeId | 卡在哪 |
|---|---|
| `beipingjiqi` | 北平机器有 **8 家分店**，没一家在鼓楼。选错会把用户导到错的酒吧 |
| `langyuan` | 郎园 Station(东坝) / Park(八宝山) / VINTAGE(建国路)，都不在亮马河边 |
| `zuoshu` | 「做書」搜不到独立 POI，只搜得到隆福寺这条街 |
| `qmex` | 搜到的全是 QMEX 墨西哥餐吧连锁，三里屯机电院那家没有。还开着吗？ |
| `zhaodai` | 完全搜不到。正式登记名是什么？ |

候选在 `backend_app/data/amap_candidates.json`（Git 忽略），
人读的版本在 `地点核对表.md`。确认一个写一个，**核对几个生效几个**，不用一次做完。

重新拉候选：`.venv/bin/python -m scripts.resolve_amap_places`

### B. 部署 Vercel

GitHub 登录 → Import 仓库 → 分支选 `codex/current-backend-foundation` →
环境变量从 `.env` 复制（Preview / Production 分开）。

**部署后第一件事：用手机 4G（不开代理）打开 Preview 链接。**
`*.vercel.app` 在国内可达性不稳定，打不开就绑 heyjue.com 的子域名。
这件事发现得越晚越糟。

### C. 手机端到端

真机 + HTTPS，走完整一遍：说一句话 → 看推荐 → 带我去 → 真的走过去 → 回来反馈。
必须真机的三个理由：麦克风权限只在 HTTPS 下给、地图跳转要唤起高德 App、
围栏要真的移动才测得出来。

**这一步没做完就不算做完。** 目前所有验证都只在电脑浏览器里做过。

---

## 6. PRD 里还没做的

| | 为什么 |
|---|---|
| FR-04 分层信号（HRV 接入） | 需要可穿戴。设置页已如实标注「还没做」 |
| FR-31 主动触达的**推送** | Web 没有可靠后台推送。产品逻辑和防打扰规则做完了（`reach-policy.js` + 13 条测试），差的是投递 |
| FR-16 空间档案生成管线 | 评论 LLM 提取 + 街景，属于里程碑 1 |
| FR-17 反馈加权覆盖估计值 | 要先有真实反馈（现在 0 条） |
| FR-13 社交破冰 / FR-21 代理度开关 | P2 |
| §8「推荐返回 < 3s」 | **本地量不准**（跨境）。部署到香港之后要重新量，超了就给高德路线加缓存 |

---

## 7. 文件地图

```
此在-current-原型.html      主原型（单文件，vanilla JS + hash 路由，~3300 行）
current-client.js           前端 API 客户端 + 语音 + 围栏（含 WGS→GCJ 转换）
reach-policy.js             主动触达/守护层的纯函数策略（有 Node 测试）
backend_app/
  main.py                   API 入口
  interpretation.py         自然语言 → NeedState（模型 + 规则兜底 + 证据护栏）
  recommender.py            我们自己的排序逻辑（不交给 LLM）
  map_provider.py           高德 POI / 步行路线 / 三家地图深链
  coords.py                 GCJ-02 ↔ WGS-84（境内差 555 米）
  opening_hours.py          营业状态三级来源
  presence.py               在场证明降级
  storage.py                入库（白名单）
  data/places.json          26 个地点（由 HTML 生成，别手改）
  data/place_overrides.json 人工核对结果 ← 21/26
tests/                      93 条 Python + 13 条 Node
开通清单-外部服务.md         四个服务怎么开、费用、坑
地点核对表.md               给人读的核对表
填Key.sh                    填 Key 用
```

---

## 8. 一句话

后端和主旅程都已经接上真实数据并在浏览器里逐屏验过。
**剩下的全部是「只有人能做」的事**：5 个地点核对、部署、真机验证。

不要只继续建不可见的后端。每完成一段，都从普通用户视角打开页面确认一遍。
