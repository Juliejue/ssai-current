# 此在 / Current —— 新对话背景说明

把这份完整贴给新对话即可接续。

---

## 产品

**此在 / Current**（SSAI 黑客松项目）。基于 AI 与真实到访反馈的**情绪—空间匹配平台**。
闭环：出发前情绪 → 需求发现 → 推荐空间 → 实际到访 → 30 秒反馈 → 私密记录／匿名贡献 → 改进下一次推荐。

- **此在** = 海德格尔 Dasein，"在此处存在"。不问你是什么样的人，只问你此刻在哪儿。
- **Current** 一词三义：此刻的 / 电流（HRV、皮电）/ 水流（江边、河岸）。

### 三条硬边界（会否决设计方案，不是价值观标语）
1. **不诊断** —— 不给情绪贴标签，不暗示病理。可以说"你 7 次里有 5 次选了人不多"，不能说"你是高敏感人群"。
2. **不评判** —— 变化分数**负端不能用红**。情绪变差不是错误，只是这次没接住。
3. **不社交** —— 没有昵称、人脸、主页、评论、关注、私信。只有用户主动匿名贡献的记录会露出。

---

## 已定稿的视觉（三份 Artifact，可直接打开）

- **品牌手册** https://claude.ai/code/artifact/42696622-8f30-457d-b7e1-26838802ed30
- **路演物料** https://claude.ai/code/artifact/f27db5ee-af22-42e6-994f-a4c4448b3cee
- **标识 A 稿收敛（含四档对比与几何论证）** https://claude.ai/code/artifact/8445e962-7efc-405c-8038-e3e887c8c440

### 标识 A·2
一条流 + 一个开口的环 + 一个交点。**环与流严格相切**，切点同时是环的最低点与流线的波峰 —— 相切意味着交集只有一个点，"此刻"没有第二个落处。环在切点处对称开口 **56°**（小于 40° 在 32px 下会闭合）。环直径 : 流线全长 = 1 : 2.4，环与流**等粗**。

### 色板（浅色体系）
```
--paper #F0F3F2   日光白 · 页面底
--card  #FFFFFF   纸 · 内容承载
--sunk  #E8EDEC   下沉面
--ink   #122A2E   深青墨（不用纯黑）
--ink-dim #5A6E70  --ink-faint #8CA0A0
--line  rgba(18,42,46,.12)
--water #1E6E72   流 · 空间 · 冷      --water-wash #DFEBEA
--sun   #C4703C   此刻 · 你 · 唯一的暖  --sun-wash #F6E9DF
```
**唯一暖色规则**：冷色承担空间与时间，**暖色只标记"你 / 此刻"**。用色比例 70 底 / 22 墨 / 6 水青 / **2 赭**。
一屏之内出现超过两处赭色就是没改干净。

### 情绪变化色阶（−3…+3，负端走灰紫，绝不用红）
```
−3 #4E4B63  −2 #6B687F  −1 #9694A5  0 #C3C9C7  +1 #86ADA8  +2 #4A8F8A  +3 #1E6E72
```

### 字体
中文标题 Songti SC ／ 中文正文 PingFang SC ／ 西文显示 Newsreader ／ 数据标签 IBM Plex Mono。
（网页里用 Noto Serif SC + Noto Sans SC，均为 OFL，商用免费，无需采购。）

### 向导「小在」
一朵云，**是标识里那个环松开之后长出来的**——环坐在流上，小在浮在流上。
**只靠眼睛做表情，没有嘴**（有嘴就会有情绪表演，而它的职责是陪着）。四种够用：平静 / 看向你 / 闭眼（记录保存、切私密）/ 陪着（旁边那个暖点是**你**，不是小在的一部分）。
文案：「小在眼中的这里」「我们的此在」（个人页）。匿名流用**「某个人的此在」**，不是"某个人的小在"。

### 内容语气
**讲"你到了做什么"，不讲"这个地方是什么"。** 没有评分、没有必去、没有氛围感，代价照实写（"真的很挤"、"门槛不是钱是应付人的力气"）。
卡片标题 = 一个动作，地名降为副标题。`会看到什么`一行专门用来**替代照片**。

---

## 文件

| 文件 | 说明 |
|---|---|
| `/Users/juer/Desktop/ssai 情绪空间项目/此在-current-原型.html` | **主原型**，单文件 ~140KB，纯 vanilla JS + hash 路由，无框架无构建 |
| `/Users/juer/Downloads/perch-栖-原型.html` | 改造前的旧版（深色 / 黑猫 / 上海地点），保留可回滚 |
| `/Users/juer/Desktop/ssai 情绪空间项目/CurrentHRVDemo/` | 队友的 Swift / HealthKit HRV demo，Phase 2 入口 |

已交付给用户的文件（在对话记录里）：标识 SVG（含小在四表情）、路演物料 7 张成品 PNG、`此在-路演模板.pptx`（六种页型，Keynote 可编辑）。

> 注意：品牌手册和路演物料的**源 HTML 在上个会话的临时目录里，新会话不存在**。Artifact 是持久记录，要改就用上面的 URL 更新（`Artifact` 工具传 `url` 参数）。

---

## 原型现状

**已完成**
- 深色 → 浅色全量改造；唯一暖色规则逐点判过（`--gold` 原 13 处，只保留 3 处暖）
- 改名：栖→此在、Perch→Current、黑猫→小在（云）
- 情绪变化色阶落地，**红色从三处负向用色中彻底移除**
- 情绪从 6 个扩到 **10 个**（刻意不用"焦虑/疲惫"这类临床标签，改用白话身体感）
- **北京空间库 26 个地点**，动作优先卡片，每条含 `action / see / cost / tags / matchReason / placeInsights`
- **12 个标准化标签**（组员 10 个 + 消费压力 + 能一直走）
- **统一因素字典 25 条**（原来每地点各写 10 条，260 条重复已消除）
- 符号参数从标签推导（`sigFromTags`），形和数据同源
- **加权匹配算法** `matchScore(place, mood, needs)`，用户明确选的诉求维度权重 ×2.8
- **需求发现页** `#/needs`，13 个诉求分空间／刺激／恢复三向，按情绪动态排序
- 验证：疗愈类情绪（低落/需要安静/累/心里发紧）top5 **无夜店混入**；加诉求组合后 24/26 个地点可进 top3

**已知故障（下个对话优先修）**
1. **`COMMUNITY` 与 `seed()` 仍引用旧上海 placeId**（`river-sunset-walk` `corner-cafe` 等）。`placeById` 兜底是 `PLACES[0]`，所以**不崩但静默错配**——所有旧记录会显示成"水果店 fRUITYSHOP"，匿名流里别人的话挂到错的地方。演示会被看出来。
2. **7 条真实原话（`p.quote`）一次都没被渲染。** 数据存了，没接进匿名到访流。
3. **卡片底部翻页圆点是 `list.map()` 生成的**，地点从 8 变 26 之后是一排 26 个点，糊成一条。
4. **`ROUTES` 四条路线数据完整但 UI 没露出**（小西天 citywalk / 鼓楼 / 三里屯 / 亮马河黄昏），没有路线页。
5. 福声唱片、火鬼 paro 在卡片模式永远进不了 top3（价值是"顺路""周二特价"这类上下文，标签体系表达不了）。瀑布流里能看到。
6. 地点的距离与交通是估算值，需核对。

---

## 正在讨论：信息密度

用户希望**整体版面减少 20–30%，但保留信息重点**。已给出诊断和三个方案，**尚未选定**：

诊断：问题不是字多，是**层级都一样重**——推荐卡 9 个区块，`为什么此刻推荐给你`／`小在眼中的这里`／`平均变化`各自一个框，视觉重量相同。
主张：**卡片 = 一个论点　瀑布流 = 一眼可比　详情页 = 全部**。现在三层都在试图说完整。

- **方案一（视觉 −25%，信息零损失）**：去掉 insights 和 avg 的框改纯文字分区，只留 why 是框；删环境标签行（与 `see`/`cost` 重复 60–70%）；"到达"移详情页；区域·类别并进地名行。9 块 → 6 块。
- **方案二（−35%）← 已推荐**：方案一 + insights 卡片只留最强一条 + avg 免责说明缩成一行。**动作、为什么此刻推荐给你、会看到什么、消费压力这四样一个不动。**
- **方案三（−55%）**：卡片只留符号+动作+地名+为什么+数字+按钮，其余全移详情页。最贴合"替我选"定位，但会看不到最值钱的细节，不推荐。

同一原则待应用到：瀑布流卡片、反馈页（10 个影响因素 → 先显示 6 个 + 展开）、需求页（每组前 4 个 + 更多）。

---

## 组员 11 条建议的处置

| 状态 | 条目 |
|---|---|
| 已做 | #5 深色→浅色 · #2b 情绪扩到 10 个 · #6「我们的此在」（只改名不改语义）· #7 地点库 · #10 标签字典 · #2c 需求发现页 |
| 已裁决**不做** | #2a 气泡云团首屏 —— 云只做小在的形象，不扩散成界面语言 |
| 折中执行 | #8 真实图片 —— 保留抽象符号 + 用 `会看到什么` 一行承担图片功能。小众场所（盲区、水果店、做書）无合规图源，硬配图会错配 |
| 待办 | #1c/#3/#9b 呼吸测量（三处同一组件，和 `CurrentHRVDemo` 对得上）· #9a 反馈多层面选项 · #11 年度回顾（现在是假数据，做出来是空的）|
| 建议先讨论 | #4 装扮付费解锁 —— 是商业化功能不是 UI；"心情不好=小乌云、开心=彩虹云"把情绪做成可消费皮肤，且乌云/彩虹自带价值判断，与"不评判"有张力。小在的四种表情已能承载状态表达 |

---

## 待确认

- **7 条原话是真人在群里说的**（含 bumble 约会场景），公开引用前需当事人同意。
- **二维码全是占位图形**，扫不出东西。真实短链定了用深青墨 `#122A2E` 重新生成，不要纯黑。
- **路演数字 `+2.4 / 1,205 份` 是原型示例值**，上台必须说明是模拟数据，不是实测结果。

---

## 工作方式

- 单文件原型，用 Bash + python 精确替换来改，改完 `node --check` 验语法，headless Chrome 截图验视觉。
- 用户时间紧、在意 token。**先给判断和取舍，再动手**；有冲突（尤其和三条边界冲突）要直说，不要默默执行。
