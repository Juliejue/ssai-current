# 七项任务审计与交付（2026-09-23）

## 结论

组员这两天真正完成的是：四城“此刻”演示内容、匿名共创的本机草稿与媒体保存、地点实景图，以及四座试点城市的定位入口。`城市及LLM模型` 文件中写到的 59 城、42 个 GPS 边界、`generate_places` 和 `score_relevance` 并不在实际运行代码中，不能算已交付。

当前运行架构本来就没有“两次 LLM 生成地点并打分”：候选地点来自高德，排序是可解释的确定性算法，LLM 只负责理解用户和为地图新发现的地点写两句文案。这比把地点事实和排序交给模型更符合 PRD 的真实性边界。因此“合并两次 LLM”按架构核验为已被更可靠方案取代，而不是再人为增加一次生成式调用。

## 七项逐条审计

| # | 任务 | 组员在两天内完成了什么 | 本轮补齐 | 当前状态 |
|---|---|---|---|---|
| 1 | 找脑环摊位负责人谈技术与合作 | 只有简要方向记录，没有访谈证据或合作结论 | 已给出 15 分钟访谈脚本、技术问题、合作阶梯和红线 | 需要本人线下执行；材料已就绪 |
| 2 | 推荐精准度、情绪空间数据库、用户共创和社群 | 完成本机匿名草稿、照片/视频本地存储、四城演示内容；没有线上共创库，也没有反馈反哺排序 | 新增空间画像表；仅满 5 次“围栏+停留”到访才形成画像；推荐读取画像并保守修正；共创新增明确提交、审核队列、删除凭证、过期规则和审核后公开列表 | 代码完成，需上线迁移后产生真实数据 |
| 3 | GIS/算法调研与补充技术成员 | 有四城素材和零散技术说明，没有可执行技术路线或招募画像 | 已形成地理推荐三阶段路线、指标、候选人画像、面试题和渠道建议 | 调研材料完成；联系候选人需团队执行 |
| 4 | 合并 `generate_places` 与 `score_relevance` | 文档声称存在，实际代码没有这两个调用 | 核验实际链路：高德候选 + 确定性排序 + 单独文案；拒绝把地点事实/排序重新交给 LLM | 已由现有架构取代，无需改成一次 LLM |
| 5 | 缩短 prompt 和输出 token | 未完成 | 理解、文案、到访反思三条调用都增加输出上限；压缩理解和文案系统提示；保留安全与结构约束；提示词版本号已更新 | 完成 |
| 6 | 更多城市 GPS 反查边界 | 只有北京、上海、广州、深圳四个矩形 | 不继续堆易错矩形；四城矩形只做降级保底 | 被第 7 项的精确反查取代 |
| 7 | 接真实地图 API 精确城市反查 | 未完成 | 新增高德逆地理编码；浏览器定位转换到高德坐标后，服务端只返回行政城市，不保存坐标；成都等非四城位置也可正常进入附近搜索 | 完成 |

## 推荐准确度现在如何闭环

1. 用户原话被转换成受限的需求状态；明确说出的活动比情绪推断优先。
2. 高德按当前位置与需求关键词找真实 POI；人工地点与动态地点明确区分来源。
3. 确定性算法按需求、精力、距离、社交、预算、营业状态与明确活动排序。
4. 路线用高德实测，重新校正原先的直线/原型距离，防止“看起来近、实际走两小时”。
5. 只有围栏内停留得到验证的反馈可以进入空间画像；少于 5 次不显示、不影响排名。
6. 达到门槛后，反馈最多只占标签修正的 45%，避免早期少量样本把人工核对资料冲掉。
7. 推荐卡会标明属性来自人工估计、地图类别估计还是已验证反馈，并显示样本量。

## 共创与社群现在的边界

- “只存本机”和“提交审核”是两个不同按钮，默认不会上传。
- 提交内容先进入 `pending`，没有自动公开，也不建立昵称、主页、关注或私信。
- 审核通过的线索才出现在公开列表；限时内容会自动过期。
- 图片和视频仍只在本机保存，服务端只记录媒体数量，避免在没有内容安全与对象存储前偷偷上传。
- 每次成功提交返回只在用户设备保存的删除凭证；服务端只保存其哈希。
- 运营人员可用 `scripts/moderate_contributions.py` 查看、通过或拒绝待审内容。

## 还需要真实世界完成的两件事

1. 按《脑环摊位访谈与合作清单》与设备负责人面谈，并拿到 SDK、原始数据能力、采样/延迟/伪迹与授权边界的明确答案。
2. 按《地理推荐技术路线与技术合伙人画像》发出招募并做技术面谈。产品现在能继续测试，但若要扩到多城并建立长期数据壁垒，需要一名真正做过地理数据或推荐系统的人共同负责。
