# 北上广深与地点数据源实施说明

更新时间：2026-09-22

## 结论

北上广深可以打通，技术难点不在“多加三个城市按钮”，而在每座城市都要有经过核对的地点身份、坐标、照片、营业状态和适合情绪场景的产品文案。

本轮已经完成四城的产品骨架：接力卡片和投稿城市可切换北京、上海、广州、深圳；定位模块能识别四个试点城市；每城有明确标注为“演示、非实时”的接力内容。常规地点推荐库目前仍以北京人工核对数据为主，不能把四城入口误说成四城地点库已经同样完整。

## 数据源判断

| 数据源 | 可行性 | 适合获取 | 当前建议 |
| --- | --- | --- | --- |
| 高德地图 Web Service | 高 | POI 名称、唯一 ID、坐标、地址、类型、照片、周边检索、路线 | 作为主地图源，通过官方 API 获取，人工核对后进入正式库 |
| 百度地图开放平台 | 中高 | POI 检索、详情、评分、营业时间、坐标转换 | 作为第二来源和缺失项补充；先申请 AK，并处理 BD-09 / GCJ-02 坐标差异 |
| 大众点评 | 不建议直接爬取 | 评论、评分、消费体验、用户照片 | 不做未授权自动抓取；仅在取得书面许可、正式合作接口或人工合法引用后使用 |
| 用户投稿 | 高，但需治理 | 此刻画面、安静程度、短时变化、真实体验 | 账号、审核、举报、版权确认上线后再进入公共内容流 |

高德官方地点搜索文档：https://lbs.amap.com/api/webservice/guide/api/search/

百度地点检索官方文档：https://lbsyun.baidu.com/docs/webapi?title=placev3%2Fguide%2Fwebservice-placeapiV3%2Fuse

美团用户服务协议明确要求：未经事先书面许可，不应通过未经认可的程序或非正常浏览方式抓取平台内容。因此大众点评不应作为当前版本的爬虫数据源：https://rules-center.meituan.com/m/detail/4

## 当前已经验证的高德流程

- 基础地点目录共有 26 个地点。
- `place_overrides.json` 中有 21 个已人工核对的高德 POI；其中 20 个已经取得照片并映射到前端。
- `scripts/resolve_amap_places.py` 只生成候选，不会自动覆盖正式数据。
- `scripts/fetch_place_photos.py` 只给 `verification_status == "verified"` 的 POI 拉取照片。
- `scripts/build_place_photos.mjs` 将审核后的照片清单生成给静态原型使用。

这说明“通过高德官方 API 获取地点和照片”已经跑通；尚未跑通的是上海、广州、深圳各自的批量选点、人工核对和产品文案，不是接口本身。

## 四城完整上线的建议顺序

1. 每城先做 12–20 个种子地点，覆盖安静、绿色、可行走、室内、夜间、免费/低消费等核心情境。
2. 用高德 API 拉候选和照片，只把人工确认过名称、地址、坐标的 POI 写入正式覆盖层。
3. 百度只用于高德缺失项的交叉核验；记录来源与坐标系，不直接混入同一坐标字段。
4. 大众点评评论和用户图片不自动搬运。需要体验证据时，优先使用现场采集、用户授权投稿或合作授权数据。
5. 上线前逐城抽查闭店、搬迁、照片错配、路线与营业时间；保留“数据更新时间”和纠错入口。

## 当前阻塞项

本地项目没有配置 `AMAP_WEB_SERVICE_KEY`，所以这次不能继续批量拉取上海、广州、深圳的真实 POI。拿到官方 Key 后，现有脚本可以复用；新增城市数据仍应保留人工审核，不应按搜索排名自动入库。
